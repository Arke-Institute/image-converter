{
  "$schema": "https://raw.githubusercontent.com/cloudflare/workers-sdk/main/packages/wrangler/config-schema.json",
  "name": "image-converter-klados",
  "main": "src/index.ts",
  "compatibility_date": "2024-12-30",
  "compatibility_flags": ["nodejs_compat"],

  // Enable observability for better debugging
  "observability": {
    "enabled": true
  },

  // Durable Object binding
  "durable_objects": {
    "bindings": [
      { "name": "KLADOS_JOB", "class_name": "KladosJobDO" }
    ]
  },

  // SQL storage for DO (required for SQL-backed DOs)
  "migrations": [
    { "tag": "v1", "new_sqlite_classes": ["KladosJobDO"] }
  ],

  // Environment variables (non-secret)
  "vars": {
    // Default agent ID (fallback, using main network ID)
    "AGENT_ID": "01KJAZMNZ3YRX46HWG2V55NXQC",
    "AGENT_VERSION": "0.1.0",

    // Network-specific agent IDs for dual-network deployment
    "AGENT_ID_TEST": "IIKJ841TCDPYVPTDNZ3Z1JW067",
    "AGENT_ID_MAIN": "01KJAZMNZ3YRX46HWG2V55NXQC",

    "LAMBDA_URL": "https://vfraisk65npyayzhf44ukio2fe0jghzc.lambda-url.us-east-1.on.aws"
  }

  // Secrets (set via `wrangler secret put`):
  // - ARKE_AGENT_KEY: Default agent API key (fallback)
  // - ARKE_AGENT_KEY_TEST: Test network klados key
  // - ARKE_AGENT_KEY_MAIN: Main network klados key
  // - LAMBDA_SECRET: Lambda authentication secret
}
